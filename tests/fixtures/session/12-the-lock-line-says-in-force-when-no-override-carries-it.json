{
  "name": "the-lock-line-says-in-force-when-no-override-carries-it",
  "direction": "record",
  "description": "BL12/T375, the other direction and the deliberate twin of fixture 11 - the SAME arming file, run with no override in the environment. The state segment reads 'policy lock - in force' and nothing precedes it, which is the only spelling that means the lock governs this session's next write. The pair is what makes either fixture evidence: 11's expected text is not a substring of 12's and 12's is not a substring of 11's, so neither could pass by rendering the other's line, and a change that reported one state for both worlds fails one of them",
  "files": [
    {
      "path": ".keel/keel-policy.md",
      "text": "---\ntier: 2\n---\n\n# keel policy - fixture project\n\n## Policy lock\n\nrelax:\n- release-version-bump\n\nworkshop:\n- hooks/\n"
    }
  ],
  "invocations": [
    {
      "sub": "session",
      "stdin": {
        "hook_event_name": "SessionStart",
        "source": "startup",
        "session_id": "$SESSION",
        "cwd": "$PROJECT"
      }
    }
  ],
  "expect_exit": 0,
  "expect_events": ["session_start"],
  "expect_stdout_contains": [
    "policy lock - in force",
    "relax: release-version-bump",
    "workshop: hooks/",
    "KEEL_OVERRIDE=(unset)"
  ],
  "expect_audit_absent": ["$USERNAME"]
}
